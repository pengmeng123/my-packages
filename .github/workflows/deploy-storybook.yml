# 自动部署 Storybook 组件文档到 GitHub Pages
# 触发条件：推送到 main 分支 或 手动触发
# 访问地址：https://pengmeng123.github.io/my-packages/storybook/

name: Deploy Storybook

on:
  push:
    branches: [main]
  workflow_dispatch: # 支持手动触发

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      # 1. 拉取代码
      - name: Checkout
        uses: actions/checkout@v4

      # 2. 安装 Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      # 3. 安装 pnpm
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      # 4. 安装依赖
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 5. 构建 Storybook
      - name: Build Storybook
        working-directory: packages/ui-components
        run: pnpm run build:docs

      # 6. 移动到 /storybook 目录
      - name: Move to storybook folder
        run: |
          mkdir -p site/storybook
          mv packages/ui-components/storybook-static/* site/storybook/

      # 7. 配置 GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v4

      # 8. 上传构建产物
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      # 9. 部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
